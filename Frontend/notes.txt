url api:
http://localhost:3000/api/cameras

A faire: 
_mocha (test js)
_version mobile
_liste panier (n'écrase pas les produits precedents)
_ne pas pouvoir ajouter de produit sans option, 
_ajouter plusieurs produits

question mentor: 
_condition addToCart-btn ne marche pas
_products pas défini alors que "local_storage"
_j'ai l'impression de ne pas rentrer dans la condition panier vide


  for (j = 0, j < local_storage.lenght, j++) {
      console.log(local_storage.lenght)
     document.querySelector(".containerAffichagePanier").innerHTML += `
     <h2>Vôtre panier</h2>
        <div id="petitContainerAffichagePanier">
        </--exemple-->
          <p>price= ${local_storage[j].price}</p>
        </div>
     `;
  }


 Pour update le panier utiliser json parse pour changer le local storage 
 en objet javascript puis set item afin de refaire le panier pour le mettre à jour